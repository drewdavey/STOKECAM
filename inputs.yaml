
num_frames: 10        # Number of frames to be captured by numFrames mode
dt: 0                 # Time between frames in seconds 


calib_on_boot: false   # Ask to calibrate on boot?
calib_frames: 10       # Number of frames to be captured by calib mode

launch_standby: true  # Launch into standby mode on boot?

shooting_mode: 'bright'

